import React from 'react';
import { SumateHome, SumateAntecedentes, SumateInfo, SumateInst, SumateUni, SumateHow, SumateConoce } from '@/components/organisms';

const SumatePage: React.FC = () => {
    return (
        <main>
            <SumateConoce />
        </main>
    );
};

export default SumatePage;